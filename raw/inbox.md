# Inbox — quick capture

Append new thoughts, links, and notes **on top** (Karpathy's append-and-review style).
Run `/process-inbox` (or `make process-inbox` / `./sb.ps1 process-inbox`) to triage these into the wiki.
Lines starting with `#` are ignored.

---
