streamlit>=1.40

# --- Search / RAG layer (optional; the rest of the app works without these) ---
rank-bm25>=0.2.2            # lexical BM25
numpy>=1.24
sentence-transformers>=3.0  # local embeddings + cross-encoder rerank (pulls torch)

# Answer providers (install at least the one you use)
openai>=1.40               # generation + logprobs/perplexity
anthropic>=0.49            # generation (no logprobs → no perplexity)
